# Deployment configuration for cloud platforms

# Heroku
web: streamlit run deploy/app.py --server.port=$PORT --server.address=0.0.0.0

# General purpose
streamlit: streamlit run deploy/app.py --server.port=8501 --server.address=0.0.0.0
